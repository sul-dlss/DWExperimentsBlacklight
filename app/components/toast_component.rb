# frozen_string_literal: true

# A single Bootstrap toast notification. Cloned client-side by the
# toast-container controller in response to `show-toast` events.
class ToastComponent < ViewComponent::Base
end
